// Window-level keyboard shortcuts. Currently empty — the legacy ♭/♮/♯
// layout cycle (ArrowLeft/Right) and the legacy seam-shift (ArrowUp/Down)
// were removed when refSpine took over layout positioning and the uniform
// septimal mode dropped the shift parameter. File kept as a stable entry
// point for future global shortcuts.
