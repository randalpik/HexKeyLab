// HexKeyLab — entry point. All wiring lives in ui/init.ts; pulling that in
// triggers the bootstrap (audio init, MIDI access, canvas listeners, first
// paint, window-bridge for inline HTML handlers).

import './ui/init.js';
